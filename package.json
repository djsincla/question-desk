{
  "name": "question-desk",
  "private": true,
  "description": "Development tooling only. The web app itself has no dependencies.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browsers": "node --test browser-tests/*.test.js",
    "test:visual": "node --test browser-tests/visual.test.js",
    "test:visual:update": "QD_UPDATE_BASELINES=1 node --test browser-tests/visual.test.js",
    "wp:build": "node wordpress/build.js",
    "wp:start": "node wordpress/build.js && wp-env start",
    "wp:test": "node wordpress/build.js && wp-env run tests-cli --env-cwd=wp-content/plugins/question-desk composer update --quiet && wp-env run tests-cli --env-cwd=wp-content/plugins/question-desk vendor/bin/phpunit",
    "wp:browsers": "node --test wordpress/browser-tests/*.test.js"
  },
  "devDependencies": {
    "@wordpress/env": "11.15.0",
    "jsqr": "1.4.0",
    "pixelmatch": "5.3.0",
    "playwright": "1.63.0",
    "pngjs": "7.0.0"
  }
}
